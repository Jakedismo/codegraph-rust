# Example secrets file (DO NOT COMMIT REAL SECRETS)
[secrets]
openai_api_key = "sk-..."
jwt_secret = "your-jwt-secret"

# Encrypt this file with:
#   CONFIG_ENC_KEY=$(cargo run -p measure_baseline_performance --bin config_tool -- generate-key)
#   cargo run -p measure_baseline_performance --bin config_tool -- encrypt --input config/secrets.toml.example --output config/secrets.enc
