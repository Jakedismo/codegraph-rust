[package]
name = "semchunk-rs"
version = "0.1.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Lightweight semantic chunker used offline"

[lib]
name = "semchunk_rs"
path = "src/lib.rs"

[dependencies]
