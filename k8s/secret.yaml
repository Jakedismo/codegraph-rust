apiVersion: v1
kind: Secret
metadata:
  name: codegraph-api-secrets
  namespace: codegraph
  labels:
    app.kubernetes.io/name: codegraph-api
    app.kubernetes.io/part-of: codegraph
type: Opaque
stringData:
  # Optional: used by vector providers if enabled
  OPENAI_API_KEY: ""

