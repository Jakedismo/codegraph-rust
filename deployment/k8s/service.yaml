apiVersion: v1
kind: Service
metadata:
  name: codegraph-api
  namespace: codegraph
  labels:
    app.kubernetes.io/name: codegraph-api
    app.kubernetes.io/part-of: codegraph
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: codegraph-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP

